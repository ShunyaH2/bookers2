<!DOCTYPE html>
<html>
  <head>
    <title>Bookers2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script src="https://kit.fontawesome.com/ddeb48cbe9.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <% if user_signed_in? %>
        <li><%= link_to "Bookers", root_path %></li>
        <li><%= link_to "Home", my_books_books_path %></li>
        <li><%= link_to "Users", users_path %></li>
        <li><%= link_to "Books", books_path %></li>
        <li><%= link_to "Log out", destroy_user_session_path, method: :delete %></li>
      <% else %>
        <li><%= link_to "Bookers", root_path %></li>
        <li><%= link_to "Home", root_path %></li>
        <li><%= link_to "About", about_path %></li>
        <li><%= link_to "Sign up", new_user_registration_path %></li>
        <li><%= link_to "Log in", new_user_session_path %></li>
      <% end %>
    </header>

    <% flash.each do |key, message| %>
      <% next if key.to_s == "errors" %>  <!-- ★errors はここで出さない -->
      <div class="alert alert-<%= key == "notice" ? "success" : "danger" %>">
        <%= message %>
      </div>
    <% end %>
    
    <%# ★ 詳細エラーはここだけで表示（1か所だけ） %>
    <% if (errs = (flash.now[:errors] || flash[:errors])).present? %>
      <% errs = Array(errs) %>
      <div class="alert alert-danger">
        <h4><%= pluralize(errs.size, "error") %> prohibited this action:</h4>
        <ul class="mb-0">
          <% errs.each do |msg| %><li><%= msg %></li><% end %>
        </ul>
      </div>
    <% end %>
    
    <div class="container-fluid">
      <div class="row">
        <%# 左サイドバー: ログイン済み かつ トップ(=homes#top)以外で表示 %>
        <% if user_signed_in? &&
      !(controller_name == "homes"  && action_name == "top") &&
      !(controller_name == "users"  && %w(edit update).include?(action_name)) &&
      !(controller_name == "books"  && %w(edit update).include?(action_name)) %>
  <aside class="col-md-3 col-lg-3 mb-4">
    <%= render "users/info", user: current_user %>
    <%= render "books/form", book: @book %>
  </aside>
  <main class="col-md-9 col-lg-9"><%= yield %></main>
<% else %>
  <main class="col-12"><%= yield %></main>
<% end %>
      </div>
    </div>

    <footer>
      <p>CopyRight infratop.inc</p>
    </footer>
  </body>
</html>
